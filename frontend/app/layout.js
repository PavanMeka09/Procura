import './globals.css';

export const metadata = { title: 'Procura — Autonomous procurement', description: 'Policy-bounded autonomous procurement control room' };

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
